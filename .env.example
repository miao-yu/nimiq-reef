# Where the grove engine (node RPC + MySQL) is reachable. Server-side only.
GROVE_API_URL=http://127.0.0.1:5980
GROVE_API_SECRET=

# Public origin of this app, used for share links and the sign-in challenge.
NEXT_PUBLIC_APP_ORIGIN=http://localhost:3000
